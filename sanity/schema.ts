import {type SchemaTypeDefinition} from 'sanity'
import destination from './schemas/destination'
import tourPackage from './schemas/tourPackage'

export const schemaTypes: SchemaTypeDefinition[] = [destination, tourPackage]
