rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow uploads only for authenticated users
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
